import UIKit
/*
final class AppImages {
    
    private var backgroundImage: UIImageView {
        let backgroundImage = UIImageView()
        backgroundImage.image = UIImage(named: "backgroundImage")
        return backgroundImage
    }
    private var branding: UIImageView {
        let branding = UIImageView()
        branding.image = UIImage(named: "branding")
        return branding
    }
     init() {
        addSubView()
    }
}
    

private func addSubView() {
    let arr = [backgroundImage, branding]
    for _ in arr {
            // Здесь вы должны добавить view как подвид к вашему классу AppImages или другому UIView
            // Например, для добавления в основной UIView:
            self.view.addSubview(view)
        }
    }
*/
